/// Catálogo de produtos da Brand Legacy — string livre no banco (não enum)
/// para permitir adicionar produtos futuros sem migração de schema.
export const PRODUCTS = ["Club", "Tração", "Imersão", "Summit"] as const;
